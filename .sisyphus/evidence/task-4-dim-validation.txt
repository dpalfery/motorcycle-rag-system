21:    The embedder enforces 1536-dimensional output to match the Azure AI Search
22:    index (VectorSearchDimensions = 1536).  Qwen3-Embedding-4B natively produces
24:    to 1536 without quality loss.
30:        self._dims: int = 1536
38:        """Return a 1536-dimensional embedding for *text*.
41:        Raises ``ValueError`` if the returned vector length != 1536 after
56:                if len(vector) > self._dims:
59:                if len(vector) != self._dims:
60:                    raise ValueError(f"Expected {self._dims} dims, got {len(vector)}")
64:            except ValueError:
